// API Configuration
export const API_BASE_URL = 'http://127.0.0.1:8000';  // Removed trailing slash to avoid double slashes in URLs

// Other configuration constants can be added here
export const DEFAULT_HEADERS = {
    'Cache-Control': 'no-cache, no-store, must-revalidate',
    'Pragma': 'no-cache',
    'Expires': '0'
}; 